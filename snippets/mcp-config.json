{
  "//": "Fabler x402 Tools MCP server — inspect-only by default. Paid calls return a structured HTTP 402 challenge without signing a transaction. Optional auto-pay requires X402_BUYER_PRIVATE_KEY; use only a dedicated low-balance wallet funded with what you are willing to spend, never a primary wallet.",
  "mcpServers": {
    "fabler-x402-tools": {
      "command": "npx",
      "args": ["-y", "https://github.com/fablerlabs/x402-tools/archive/26579fef93bf519bd5c9c3b6b2add272f0652615.tar.gz"]
    }
  }
}
